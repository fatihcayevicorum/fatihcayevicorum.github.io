import{initializeApp}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js";
import{getAuth,onAuthStateChanged,signOut}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
import{collection,doc,getFirestore,onSnapshot}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
import{firebaseConfig}from"../firebase-config.js";
import{hasPanelAccess}from"../admin-access.js";

const app=initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app),$=id=>document.getElementById(id);
const salesCol=collection(db,"adminSales"),closingsCol=collection(db,"adminDailyClosings"),stocksCol=collection(db,"adminStockItems"),stockMovesCol=collection(db,"adminStockMovements"),internalConsumptionsCol=collection(db,"adminInternalConsumptions"),menuRef=doc(db,"publicMenu","catalog"),settingsRef=doc(db,"adminAppSettings","pos");
const el={periodButtons:$("periodButtons"),dateRange:$("dateRange"),startDate:$("startDate"),endDate:$("endDate"),applyRange:$("applyRange"),periodLabel:$("periodLabel"),connection:$("connection"),salesTotal:$("salesTotal"),cashTotal:$("cashTotal"),transferTotal:$("transferTotal"),creditTotal:$("creditTotal"),itemCount:$("itemCount"),orderCount:$("orderCount"),giftCount:$("giftCount"),tipTotal:$("tipTotal"),roundingTotal:$("roundingTotal"),merchantMarkaCount:$("merchantMarkaCount"),merchantDeliveredTea:$("merchantDeliveredTea"),categoryTabs:$("categoryTabs"),productSearch:$("productSearch"),productReport:$("productReport"),productEmpty:$("productEmpty"),rankingList:$("rankingList"),rankingEmpty:$("rankingEmpty"),categoryReport:$("categoryReport"),purchaseProductCount:$("purchaseProductCount"),purchasePackageCount:$("purchasePackageCount"),purchaseUnitCount:$("purchaseUnitCount"),allPurchaseTotal:$("allPurchaseTotal"),purchaseList:$("purchaseList"),purchaseEmpty:$("purchaseEmpty"),internalRecordCount:$("internalRecordCount"),internalProductCount:$("internalProductCount"),internalQuantity:$("internalQuantity"),internalConsumptionList:$("internalConsumptionList"),internalConsumptionEmpty:$("internalConsumptionEmpty"),stockProductSelect:$("stockProductSelect"),stockReportHint:$("stockReportHint"),stockReportContent:$("stockReportContent"),stockPackages:$("stockPackages"),stockReceived:$("stockReceived"),stockPurchaseTotal:$("stockPurchaseTotal"),stockAverageCost:$("stockAverageCost"),stockSold:$("stockSold"),stockRevenue:$("stockRevenue"),stockProfit:$("stockProfit"),stockRemaining:$("stockRemaining"),stockEntryCount:$("stockEntryCount"),stockEntryList:$("stockEntryList"),stockEntryEmpty:$("stockEntryEmpty"),closingList:$("closingList"),closingEmpty:$("closingEmpty"),closingDetailDialog:$("closingDetailDialog"),closingDetailTitle:$("closingDetailTitle"),closingDetailPeriod:$("closingDetailPeriod"),closingDetailSummary:$("closingDetailSummary"),closingDetailProducts:$("closingDetailProducts"),closingDetailEmpty:$("closingDetailEmpty"),closingProductCount:$("closingProductCount"),closingDetailPdf:$("closingDetailPdf"),closingDetailExcel:$("closingDetailExcel"),closeClosingDetail:$("closeClosingDetail"),closeClosingDetailBottom:$("closeClosingDetailBottom"),exportPdf:$("exportPdf"),exportExcel:$("exportExcel"),logoutButton:$("logoutButton"),toast:$("toast")};
let sales=[],closings=[],stocks=[],stockMovements=[],internalConsumptions=[],catalog={categories:[],items:[]},period="today",category="all",range={start:today(),end:today()},currentBusinessDate=today(),currentBusinessDayStartedAtMs=0,report=emptyReport(),selectedClosing=null,loaded=0,toastTimer;

el.periodButtons.onclick=e=>{const button=e.target.closest("[data-period]");if(!button)return;period=button.dataset.period;[...el.periodButtons.children].forEach(x=>x.classList.toggle("active",x===button));el.dateRange.hidden=period!=="custom";if(period!=="custom"){range=periodRange(period);render()}};
el.applyRange.onclick=()=>{if(!el.startDate.value||!el.endDate.value)return show("Başlangıç ve bitiş tarihini seçin.");if(el.startDate.value>el.endDate.value)return show("Başlangıç tarihi bitişten sonra olamaz.");range={start:el.startDate.value,end:el.endDate.value};render()};
el.categoryTabs.onclick=e=>{const button=e.target.closest("[data-category]");if(!button)return;category=button.dataset.category;renderProducts()};
el.productSearch.oninput=renderProducts;
el.stockProductSelect.onchange=renderStockReport;
el.closingList.onclick=e=>{const row=e.target.closest("[data-closing-id]");if(row)openClosingDetail(row.dataset.closingId)};
el.closeClosingDetail.onclick=closeClosingDetail;
el.closeClosingDetailBottom.onclick=closeClosingDetail;
el.closingDetailPdf.onclick=exportClosingDetailPdf;
el.closingDetailExcel.onclick=exportClosingDetailExcel;
el.exportPdf.onclick=exportPdf;
el.exportExcel.onclick=exportExcel;
el.logoutButton.onclick=async()=>{await signOut(auth);location.replace("../yonetici-giris.html")};
document.addEventListener("click",e=>{const menu=document.querySelector(".panel-menu");if(menu?.open&&!menu.contains(e.target))menu.removeAttribute("open")});
updateClock();setInterval(updateClock,1000);el.startDate.value=range.start;el.endDate.value=range.end;

onAuthStateChanged(auth,async user=>{if(!user){location.replace("../yonetici-giris.html?next=raporlar/");return}if(!await hasPanelAccess(user,db,"reports")){location.replace("../yonetici-giris.html");return}subscribe()});
function subscribe(){
  onSnapshot(salesCol,s=>{sales=s.docs.map(d=>({id:d.id,...d.data()}));ready()},fail);
  onSnapshot(closingsCol,s=>{closings=s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>String(b.businessDate||b.id).localeCompare(String(a.businessDate||a.id)));ready()},fail);
  onSnapshot(menuRef,s=>{const d=s.data()||{};catalog={categories:Array.isArray(d.categories)?d.categories:[],items:Array.isArray(d.items)?d.items:[]};ready()},fail);
  onSnapshot(stocksCol,s=>{stocks=s.docs.map(d=>({id:d.id,...d.data()}));ready()},fail);
  onSnapshot(stockMovesCol,s=>{stockMovements=s.docs.map(d=>({id:d.id,...d.data()}));ready()},fail);
  onSnapshot(internalConsumptionsCol,s=>{internalConsumptions=s.docs.map(d=>({id:d.id,...d.data()}));ready()},fail);
  onSnapshot(settingsRef,s=>{const d=s.data()||{};currentBusinessDate=d.currentBusinessDate||today();currentBusinessDayStartedAtMs=Number(d.currentBusinessDayStartedAtMs)||0;if(period==="today"){range={start:currentBusinessDate,end:currentBusinessDate};el.startDate.value=range.start;el.endDate.value=range.end}ready()},fail);
}
function ready(){loaded++;el.connection.innerHTML='<i class="fa-solid fa-circle-check"></i> Canlı veri';el.connection.classList.remove("error");render()}
function fail(error){console.error(error);el.connection.innerHTML='<i class="fa-solid fa-triangle-exclamation"></i> Bağlantı hatası';el.connection.classList.add("error");show("Rapor verileri alınamadı.")}

function buildReport(){
  const selected=sales.filter(s=>{const date=s.businessDate||dateFromTimestamp(s.closedAt||s.createdAt),inRange=date>=range.start&&date<=range.end;if(!inRange)return false;if(range.start===currentBusinessDate&&range.end===currentBusinessDate)return reportTime(s)>=currentBusinessDayStartedAtMs;return true});
  const result=emptyReport(),products=new Map();
  for(const sale of selected){
    if(["merchant-topup","merchant-topup-correction"].includes(sale.recordType)){result.merchantMarkaCount+=num(sale.markaCount);result.merchantTopupTotal+=num(sale.saleTotal);result.salesTotal+=num(sale.saleTotal);result.cashTotal+=num(sale.cashAmount);result.transferTotal+=num(sale.transferAmount);continue}
    if(sale.recordType==="merchant-delivery"){result.merchantDeliveredTea+=num(sale.deliveredTeaCount);continue}
    if(["payment","credit-payment","credit-topup"].includes(sale.recordType)){result.cashTotal+=num(sale.cashAmount??sale.amount);result.transferTotal+=num(sale.transferAmount);result.tipTotal+=num(sale.tipAmount);continue}
    if(sale.recordType!=="sale"&&sale.items==null)continue;
    result.salesTotal+=num(sale.baseTotal)||sumBillableItems(sale.items);
    result.cashTotal+=num(sale.cashAmount??sale.paymentAmount??sale.paidTotal);
    result.creditTotal+=num(sale.openAccountAmount);result.transferTotal+=num(sale.transferAmount);
    result.tipTotal+=num(sale.tipAmount);
    result.roundingTotal+=num(sale.roundingDiscount)||Math.max(0,-num(sale.roundingAmount));
    result.orderCount++;
    for(const item of sale.items||[]){
      const qty=num(item.quantity),price=num(item.unitPrice);
      if(item.complimentary){result.giftCount+=qty;continue}
      result.itemCount+=qty;
      const id=item.id||`${item.name}|${price}`,menuItem=catalog.items.find(x=>x.id===item.id),categoryId=menuItem?.categoryId||"other",current=products.get(id)||{id,name:item.name||menuItem?.name||"Ürün",quantity:0,total:0,categoryId};
      current.quantity+=qty;current.total+=qty*price;products.set(id,current);
    }
  }
  result.products=[...products.values()].sort((a,b)=>b.quantity-a.quantity||b.total-a.total);
  return result;
}
function render(){
  report=buildReport();
  el.periodLabel.textContent=`${formatDate(range.start)}${range.start===range.end?"":` – ${formatDate(range.end)}`}`;
  el.salesTotal.textContent=money(report.salesTotal);el.cashTotal.textContent=money(report.cashTotal);el.transferTotal.textContent=money(report.transferTotal);el.creditTotal.textContent=money(report.creditTotal);el.itemCount.textContent=formatNumber(report.itemCount);el.orderCount.textContent=formatNumber(report.orderCount);el.giftCount.textContent=formatNumber(report.giftCount);el.tipTotal.textContent=money(report.tipTotal);el.roundingTotal.textContent=money(report.roundingTotal);el.merchantMarkaCount.textContent=`${formatNumber(report.merchantMarkaCount)} Marka`;el.merchantDeliveredTea.textContent=`${formatNumber(report.merchantDeliveredTea)} Çay`;
  renderTabs();renderProducts();renderRanking();renderCategories();renderPurchases();renderInternalConsumptions();renderStockOptions();renderStockReport();renderClosings();
}
function renderTabs(){const cats=catalog.categories.filter(c=>report.products.some(p=>p.categoryId===c.id));el.categoryTabs.innerHTML=`<button class="${category==="all"?"active":""}" data-category="all">Tümü</button>`+cats.map(c=>`<button class="${category===c.id?"active":""}" data-category="${esc(c.id)}">${esc(c.name)}</button>`).join("")}
function renderProducts(){const q=el.productSearch.value.trim().toLocaleLowerCase("tr-TR"),items=report.products.filter(p=>(category==="all"||p.categoryId===category)&&p.name.toLocaleLowerCase("tr-TR").includes(q));el.productReport.innerHTML=items.map(p=>`<div class="product-row"><div class="product-copy"><strong>${esc(p.name)}</strong><span>${esc(categoryName(p.categoryId))}</span></div><b>${formatNumber(p.quantity)} Adet</b><em>${money(p.total)}</em></div>`).join("");el.productEmpty.hidden=items.length>0}
function renderRanking(){const top=report.products.slice(0,5),least=[...report.products].sort((a,b)=>a.quantity-b.quantity||a.total-b.total).slice(0,5),max=top[0]?.quantity||1,row=(p,i)=>`<div class="ranking-item"><span>${i+1}</span><div><strong>${esc(p.name)}</strong><small>${Math.round(p.quantity/max*100)}% Satış yoğunluğu</small></div><b>${formatNumber(p.quantity)} Adet</b></div>`;el.rankingList.innerHTML=top.length?`<p class="ranking-group-title">En çok satan 5 Ürün</p>${top.map(row).join("")}<p class="ranking-group-title">En az satan 5 Ürün</p>${least.map(row).join("")}`:"";el.rankingEmpty.hidden=top.length>0}
function renderCategories(){const map=new Map();for(const p of report.products){const key=p.categoryId||"other",x=map.get(key)||{name:categoryName(key),quantity:0,total:0};x.quantity+=p.quantity;x.total+=p.total;map.set(key,x)}const items=[...map.values()].sort((a,b)=>b.quantity-a.quantity),max=items[0]?.quantity||1;el.categoryReport.innerHTML=items.map(x=>`<article class="category-card"><header><h3>${esc(x.name)}</h3><b>${formatNumber(x.quantity)} Adet</b></header><div class="bar"><i style="width:${Math.max(4,x.quantity/max*100)}%"></i></div><p>${money(x.total)} Ürün Toplamı</p></article>`).join("")||'<p class="empty">Seçilen dönemde kategori verisi bulunmuyor.</p>'}
function renderPurchases(){const grouped=new Map();for(const move of stockMovements){const date=movementDate(move);if((move.type!=="in"&&move.type!=="initial")||date<range.start||date>range.end)continue;const stock=stocks.find(x=>x.id===move.stockItemId)||{id:move.stockItemId,name:move.stockName||"Stok",unitsPerPackage:move.unitsPerPackage||1,packageUnit:"koli"},key=stock.id||move.stockName,current=grouped.get(key)||{name:stock.name||move.stockName||"Stok",packages:0,units:0,total:0,entries:0};current.packages+=movementPackages(move,stock);current.units+=num(move.amount);current.total+=movementPurchase(move,stock);current.entries++;grouped.set(key,current)}const items=[...grouped.values()].sort((a,b)=>b.total-a.total||a.name.localeCompare(b.name,"tr")),packages=items.reduce((sum,x)=>sum+x.packages,0),units=items.reduce((sum,x)=>sum+x.units,0),total=items.reduce((sum,x)=>sum+x.total,0);el.purchaseProductCount.textContent=formatNumber(items.length);el.purchasePackageCount.textContent=formatNumber(packages);el.purchaseUnitCount.textContent=formatNumber(units);el.allPurchaseTotal.textContent=money(total);el.purchaseList.innerHTML=items.map(x=>`<article class="purchase-row"><i class="fa-solid fa-boxes-stacked"></i><div><strong>${esc(x.name)}</strong><small>${x.entries} Stok Girişi • ${formatNumber(x.packages)} Koli • ${formatNumber(x.units)} Adet</small></div><b>${money(x.total)}</b></article>`).join("");el.purchaseEmpty.hidden=items.length>0}
function renderInternalConsumptions(){const records=internalConsumptions.filter(x=>{const date=String(x.businessDate||dateFromTimestamp(x.createdAt)||"");if(date<range.start||date>range.end)return false;if(range.start===currentBusinessDate&&range.end===currentBusinessDate&&currentBusinessDayStartedAtMs)return reportTime(x)>=currentBusinessDayStartedAtMs;return true}).sort((a,b)=>reportTime(b)-reportTime(a));const products=new Map();let total=0;for(const record of records)for(const item of record.items||[]){const name=String(item.name||"Ürün"),qty=num(item.quantity);products.set(name,(products.get(name)||0)+qty);total+=qty}el.internalRecordCount.textContent=formatNumber(records.length);el.internalProductCount.textContent=formatNumber(products.size);el.internalQuantity.textContent=`${formatNumber(total)} Adet`;el.internalConsumptionList.innerHTML=records.map(record=>{const list=(record.items||[]).map(item=>`${esc(item.name||"Ürün")} <b>${formatNumber(item.quantity)} Adet</b>`).join(" • ");return`<article class="internal-consumption-row"><i class="fa-solid fa-box-open"></i><div><strong>${formatDate(record.businessDate||dateFromTimestamp(record.createdAt))} • ${formatTime(reportTime(record))}</strong><small>${record.note?esc(record.note):"İşletme içi kullanım"}</small><span>${list||"Ürün bilgisi bulunmuyor"}</span></div><b>${formatNumber(record.totalQuantity||(record.items||[]).reduce((sum,item)=>sum+num(item.quantity),0))} Adet</b></article>`}).join("");el.internalConsumptionEmpty.hidden=records.length>0}
function renderStockOptions(){const selected=el.stockProductSelect.value,items=stocks.filter(x=>x.active!==false&&x.linkedMenuItemId).sort((a,b)=>String(a.name).localeCompare(String(b.name),"tr"));el.stockProductSelect.innerHTML='<option value="">Stok Ürünü seçin</option>'+items.map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join("");if(items.some(x=>x.id===selected))el.stockProductSelect.value=selected}
function renderStockReport(){const stock=stocks.find(x=>x.id===el.stockProductSelect.value);el.stockReportHint.hidden=!!stock;el.stockReportContent.hidden=!stock;if(!stock)return;const entries=stockMovements.filter(x=>x.stockItemId===stock.id&&(x.type==="in"||x.type==="initial")&&movementDate(x)>=range.start&&movementDate(x)<=range.end).sort((a,b)=>movementDate(a).localeCompare(movementDate(b))),packageCount=entries.reduce((sum,x)=>sum+movementPackages(x,stock),0),received=entries.reduce((sum,x)=>sum+num(x.amount),0),purchaseTotal=entries.reduce((sum,x)=>sum+movementPurchase(x,stock),0),averageCost=received>0?purchaseTotal/received:num(stock.unitCost);let sold=0,revenue=0;for(const sale of sales){const date=sale.businessDate||dateFromTimestamp(sale.closedAt||sale.createdAt);if(date<range.start||date>range.end||["payment","credit-payment","credit-topup"].includes(sale.recordType))continue;for(const item of sale.items||[]){if(item.id!==stock.linkedMenuItemId||item.complimentary)continue;const qty=num(item.quantity);sold+=qty;revenue+=qty*num(item.unitPrice)}}const profit=revenue-sold*averageCost;el.stockPackages.textContent=formatNumber(packageCount);el.stockReceived.textContent=formatNumber(received);el.stockPurchaseTotal.textContent=money(purchaseTotal);el.stockAverageCost.textContent=money(averageCost);el.stockSold.textContent=formatNumber(sold);el.stockRevenue.textContent=money(revenue);el.stockProfit.textContent=money(profit);el.stockRemaining.textContent=`${formatNumber(stock.quantity)} ${displayUnit(stock.unit||"adet")}`;el.stockEntryCount.textContent=`${entries.length} İşlem`;el.stockEntryList.innerHTML=entries.map(x=>{const packages=movementPackages(x,stock),amount=num(x.amount),paid=movementPurchase(x,stock),cost=amount>0?paid/amount:0;return`<div class="stock-entry-row"><i class="fa-solid fa-box"></i><div><strong>${formatDate(movementDate(x))}</strong><small>${x.note?esc(x.note):"Stok Girişi"}</small></div><b>${formatNumber(packages)} Koli</b><b>${formatNumber(amount)} Adet</b><b>${money(paid)} • ${money(cost)}/Adet</b></div>`}).join("");el.stockEntryEmpty.hidden=entries.length>0}
function renderClosings(){const items=closings.slice(0,40);el.closingList.innerHTML=items.map(c=>`<button class="closing-row" type="button" data-closing-id="${esc(c.id)}"><i class="fa-solid fa-calendar-check"></i><div><strong>${formatDate(c.businessDate||c.id)}</strong><small>${formatNumber(c.orderCount)} Adisyon • ${formatNumber(c.itemCount)} Ürün • ${formatNumber(c.brewedPotCount)} Demlik</small></div><b>${money(c.salesTotal)} Satış</b><b>${money(c.cashTotal)} Nakit</b><b>${money(c.openAccountTotal)} Açık Hesap</b><i class="fa-solid fa-chevron-right closing-arrow"></i></button>`).join("");el.closingEmpty.hidden=items.length>0}

function openClosingDetail(id){
  const closing=closings.find(x=>x.id===id);
  if(!closing)return show("Gün sonu kaydı bulunamadı.");
  selectedClosing=closing;
  const products=Array.isArray(closing.products)?closing.products:[];
  el.closingDetailTitle.textContent=`${formatDate(closing.businessDate||closing.id)} Gün Sonu`;
  el.closingDetailPeriod.textContent=closing.closedAtMs?`${formatDateTime(closing.businessDayStartedAtMs)} — ${formatDateTime(closing.closedAtMs)}`:"Kaydedilmiş gün sonu raporu";
  el.closingDetailSummary.innerHTML=[
    ["Toplam Satış",money(closing.salesTotal),"fa-chart-line"],
    ["Nakit Kasa",money(closing.cashTotal),"fa-money-bill-wave"],
    ["Banka Havalesi",money(closing.transferTotal),"fa-building-columns"],
    ["Açık Hesap",money(closing.openAccountTotal),"fa-address-book"],
    ["Esnaf Marka Satışı",`${formatNumber(closing.merchantMarkaCount)} Marka • ${money(closing.merchantTopupTotal)}`,"fa-ticket"],
    ["Esnafa Teslim Çay",`${formatNumber(closing.merchantDeliveredTea)} Çay`,"fa-mug-hot"],
    ["Demlenen Çay",`${formatNumber(closing.brewedPotCount)} Demlik`,"fa-mug-hot"],
    ["Bahşiş",money(closing.tipTotal),"fa-hand-holding-dollar"],
    ["Hesap yuvarlama",money(closing.roundingTotal),"fa-scale-balanced"],
    ["Kapanan Adisyon",formatNumber(closing.orderCount),"fa-receipt"],
    ["İkram",`${formatNumber(closing.giftCount)} Adet`,"fa-gift"]
  ].map(x=>`<article><i class="fa-solid ${x[2]}"></i><span>${x[0]}</span><strong>${x[1]}</strong></article>`).join("");
  el.closingProductCount.textContent=`${formatNumber(products.reduce((sum,x)=>sum+num(x.quantity),0))} Adet`;
  el.closingDetailProducts.innerHTML=products.map(p=>`<article><div><strong>${esc(p.name||"Ürün")}</strong><small>${formatNumber(p.quantity)} × ${money(p.unitPrice)}</small></div><b>${money(p.total??num(p.quantity)*num(p.unitPrice))}</b></article>`).join("");
  el.closingDetailEmpty.hidden=products.length>0;
  el.closingDetailDialog.showModal();
}
function closeClosingDetail(){el.closingDetailDialog.close();selectedClosing=null}
function exportClosingDetailPdf(){if(!selectedClosing)return;const c=selectedClosing,rows=(c.products||[]).map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.name)}</td><td>${formatNumber(p.quantity)}</td><td>${money(p.unitPrice)}</td><td>${money(p.total)}</td></tr>`).join(""),body=`<table><thead><tr><th>No</th><th>Ürün</th><th>Adet</th><th>Birim Fiyat</th><th>Toplam</th></tr></thead><tbody>${rows}</tbody></table>`;openCorporatePdf(`${formatDate(c.businessDate||c.id)} Gün Sonu Raporu`,body,[["Esnaf Marka Satışı",`${formatNumber(c.merchantMarkaCount)} Marka • ${money(c.merchantTopupTotal)}`],["Esnafa Teslim Çay",`${formatNumber(c.merchantDeliveredTea)} Çay`],["Demlenen Çay",`${formatNumber(c.brewedPotCount)} Demlik`],["Nakit Kasa",money(c.cashTotal)],["Banka Havalesi",money(c.transferTotal)],["Açık Hesap",money(c.openAccountTotal)],["Bahşiş",money(c.tipTotal)],["Yuvarlama",money(c.roundingTotal)],["Toplam Satış",money(c.salesTotal)]])}
function exportClosingDetailExcel(){if(!selectedClosing)return;const c=selectedClosing,rows=(c.products||[]).map(p=>`<Row><Cell><Data ss:Type="String">${xml(p.name)}</Data></Cell><Cell><Data ss:Type="Number">${num(p.quantity)}</Data></Cell><Cell><Data ss:Type="Number">${num(p.unitPrice)}</Data></Cell><Cell><Data ss:Type="Number">${num(p.total)}</Data></Cell></Row>`).join(""),summary=[["Demlenen Çay",num(c.brewedPotCount)],["Toplam Satış",num(c.salesTotal)],["Nakit Kasa",num(c.cashTotal)],["Banka Havalesi",num(c.transferTotal)],["Açık Hesap",num(c.openAccountTotal)],["Bahşiş",num(c.tipTotal)],["Hesap Yuvarlama",num(c.roundingTotal)],["Kapanan Adisyon",num(c.orderCount)]].map(x=>`<Row><Cell><Data ss:Type="String">${x[0]}</Data></Cell><Cell/><Cell/><Cell><Data ss:Type="Number">${x[1]}</Data></Cell></Row>`).join(""),content=`<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Worksheet ss:Name="Gün Sonu"><Table><Row><Cell><Data ss:Type="String">Ürün</Data></Cell><Cell><Data ss:Type="String">Adet</Data></Cell><Cell><Data ss:Type="String">Birim Fiyat</Data></Cell><Cell><Data ss:Type="String">Toplam</Data></Cell></Row>${rows}${summary}</Table></Worksheet></Workbook>`;download(content,`gun-sonu-${c.businessDate||c.id}.xls`,"application/vnd.ms-excel")}

function exportPdf(){const rows=report.products.map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.name)}</td><td>${esc(categoryName(p.categoryId))}</td><td>${formatNumber(p.quantity)}</td><td>${money(p.total)}</td></tr>`).join(""),body=`<table><thead><tr><th>No</th><th>Ürün</th><th>Kategori</th><th>Adet</th><th>Toplam</th></tr></thead><tbody>${rows}</tbody></table>`;openCorporatePdf(`Satış Raporu — ${formatDate(range.start)}${range.start===range.end?"":` / ${formatDate(range.end)}`}`,body,[["Esnaf Marka Satışı",`${formatNumber(report.merchantMarkaCount)} Marka • ${money(report.merchantTopupTotal)}`],["Esnafa Teslim Çay",`${formatNumber(report.merchantDeliveredTea)} Çay`],["Nakit Kasa",money(report.cashTotal)],["Banka Havalesi",money(report.transferTotal)],["Açık Hesap",money(report.creditTotal)],["Bahşiş",money(report.tipTotal)],["Yuvarlama",money(report.roundingTotal)],["İkram",`${formatNumber(report.giftCount)} Adet`],["Toplam Satış",money(report.salesTotal)]])}
function exportExcel(){const rows=report.products.map(p=>`<Row><Cell><Data ss:Type="String">${xml(p.name)}</Data></Cell><Cell><Data ss:Type="String">${xml(categoryName(p.categoryId))}</Data></Cell><Cell><Data ss:Type="Number">${p.quantity}</Data></Cell><Cell><Data ss:Type="Number">${p.total}</Data></Cell></Row>`).join(""),summary=[["Toplam Satış",report.salesTotal],["Nakit Kasa",report.cashTotal],["Banka Havalesi",report.transferTotal],["Açık Hesap",report.creditTotal],["Bahşiş",report.tipTotal],["Hesap Yuvarlama",report.roundingTotal],["İkram Adedi",report.giftCount],["Kapanan Adisyon",report.orderCount]].map(x=>`<Row><Cell><Data ss:Type="String">${x[0]}</Data></Cell><Cell/><Cell/><Cell><Data ss:Type="Number">${x[1]}</Data></Cell></Row>`).join(""),content=`<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Worksheet ss:Name="Satış Raporu"><Table><Row><Cell><Data ss:Type="String">Ürün</Data></Cell><Cell><Data ss:Type="String">Kategori</Data></Cell><Cell><Data ss:Type="String">Adet</Data></Cell><Cell><Data ss:Type="String">Toplam</Data></Cell></Row>${rows}${summary}</Table></Worksheet></Workbook>`;download(content,`satis-raporu-${range.start}-${range.end}.xls`,"application/vnd.ms-excel")}

function periodRange(type){const d=new Date(`${today()}T12:00:00`);if(type==="week"){const day=(d.getDay()+6)%7,start=new Date(d);start.setDate(d.getDate()-day);return{start:iso(start),end:iso(d)}}if(type==="month")return{start:`${today().slice(0,7)}-01`,end:today()};return{start:today(),end:today()}}
function emptyReport(){return{salesTotal:0,cashTotal:0,transferTotal:0,creditTotal:0,itemCount:0,orderCount:0,giftCount:0,tipTotal:0,roundingTotal:0,merchantMarkaCount:0,merchantTopupTotal:0,merchantDeliveredTea:0,products:[]}}
function sumBillableItems(items){return(items||[]).filter(x=>!x.complimentary).reduce((sum,x)=>sum+num(x.unitPrice)*num(x.quantity),0)}
function movementDate(x){return String(x.operationDate||dateFromTimestamp(x.createdAt)||"")}
function movementPackages(x,stock){if(num(x.packageCount)>0)return num(x.packageCount);if(x.enteredUnit&&(x.enteredUnit===stock.packageUnit||x.enteredUnit==="koli"))return num(x.enteredAmount);return num(x.amount)/Math.max(1,num(x.unitsPerPackage)||num(stock.unitsPerPackage)||1)}
function movementPurchase(x,stock){if(num(x.purchaseTotal)>0)return num(x.purchaseTotal);if(x.purchasePriceBasis==="total")return num(x.purchasePrice);if(x.purchasePriceBasis==="package")return num(x.purchasePrice)*movementPackages(x,stock);if(x.purchasePriceBasis==="unit")return num(x.purchasePrice)*num(x.amount);return 0}
function categoryName(id){return catalog.categories.find(c=>c.id===id)?.name||(id==="other"?"Diğer":"Kategorisiz")}
function today(){return new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Istanbul"}).format(new Date())}
function iso(d){return new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Istanbul"}).format(d)}
function dateFromTimestamp(v){const d=v?.toDate?.();return d?new Intl.DateTimeFormat("en-CA",{timeZone:"Europe/Istanbul"}).format(d):""}
function reportTime(v){return v.closedAt?.toMillis?.()||v.createdAt?.toMillis?.()||num(v.closedAtMs)||num(v.createdAtMs)}
function formatDate(v){const [y,m,d]=String(v||"").split("-");return y&&m&&d?`${d}.${m}.${y}`:"—"}
function formatDateTime(v){if(!num(v))return"—";return new Intl.DateTimeFormat("tr-TR",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"Europe/Istanbul"}).format(new Date(num(v)))}
function formatTime(v){if(!num(v))return"—";return new Intl.DateTimeFormat("tr-TR",{hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"Europe/Istanbul"}).format(new Date(num(v)))}
function num(v){return Number(v)||0}
function money(v){return new Intl.NumberFormat("tr-TR",{style:"currency",currency:"TRY",minimumFractionDigits:num(v)%1?2:0}).format(num(v))}
function formatNumber(v){return new Intl.NumberFormat("tr-TR",{maximumFractionDigits:2}).format(num(v))}
function displayUnit(v){const value=String(v||"");return value?value.charAt(0).toLocaleUpperCase("tr-TR")+value.slice(1).toLocaleLowerCase("tr-TR"):""}
function updateClock(){const n=new Date();$("currentTime").textContent=new Intl.DateTimeFormat("tr-TR",{hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"Europe/Istanbul"}).format(n);$("currentDate").textContent=new Intl.DateTimeFormat("tr-TR",{day:"2-digit",month:"2-digit",year:"2-digit",timeZone:"Europe/Istanbul"}).format(n).replace(/\./g,"/")}
function show(message){clearTimeout(toastTimer);el.toast.textContent=message;el.toast.classList.add("show");toastTimer=setTimeout(()=>el.toast.classList.remove("show"),3000)}
function download(content,name,type){const url=URL.createObjectURL(new Blob([content],{type})),a=document.createElement("a");a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]))}
function xml(v){return String(v??"").replace(/[<>&'"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]))}
function openCorporatePdf(title,body,totals){const popup=window.open("","_blank","width=1000,height=760");if(!popup)return show("PDF penceresi açılamadı. Açılır pencereye izin verin.");const logo=new URL("../logo.png",location.href).href,created=new Intl.DateTimeFormat("tr-TR",{dateStyle:"full",timeStyle:"short",timeZone:"Europe/Istanbul"}).format(new Date());popup.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>${esc(title)}</title><style>@page{size:A4;margin:15mm}*{box-sizing:border-box}body{margin:0;color:#342522;font:12px Arial,sans-serif}.watermark{position:fixed;inset:20% 15%;width:70%;height:60%;object-fit:contain;opacity:.045;z-index:-1}.head{display:flex;justify-content:space-between;gap:20px;border-bottom:3px solid #7d1b24;padding-bottom:12px}.brand img{width:90px}.brand p{margin:2px 0}.date{text-align:right}.title{text-align:center;color:#7d1b24;margin:22px 0 14px}table{width:100%;border-collapse:collapse}thead{display:table-header-group}th{background:#7d1b24;color:#fff}th,td{padding:9px;border:1px solid #d8c6ba;text-align:left}tbody tr:nth-child(even){background:#fff7ef}.totals{width:50%;margin:16px 0 0 auto;border:1px solid #d8c6ba}.totals div{display:flex;justify-content:space-between;padding:8px;border-bottom:1px solid #eaded6}.totals div:last-child{border:0;background:#fff2e2;color:#7d1b24;font-weight:bold}</style></head><body><img class="watermark" src="${logo}"><header class="head"><div class="brand"><img src="${logo}"><p><b>Fatih Ali Altınlı</b></p><p>0554 118 08 06</p><p>Ulukavak Mah. Osmancık Cad. No:285<br>Çorum/Merkez</p></div><div class="date"><b>Oluşturma Tarihi</b><p>${created}</p></div></header><h1 class="title">${esc(title)}</h1>${body}<section class="totals">${totals.map(x=>`<div><span>${x[0]}</span><b>${x[1]}</b></div>`).join("")}</section><script>window.onload=()=>setTimeout(()=>window.print(),350)<\/script></body></html>`);popup.document.close()}
